<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /korapp/

    # Si piden /korapp/... reescribe a la carpeta public (sin mostrar /public en la URL)
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
